<Window x:Class="QLSinhVien.ThongKe"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QLSinhVien"
        mc:Ignorable="d"
        Title="ThongKe" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dgThongKe" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Foreground" Value="Blue"/>
                    <Setter Property="Height" Value="30"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã địa chỉ" Binding="{Binding MaDiaChi}" Width="150"/>
                <DataGridTextColumn Header="Tên địa chỉ" Binding="{Binding TenDiaChi}" Width="200"/>
                <DataGridTextColumn Header="Số người" Binding="{Binding SoNguoi}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
