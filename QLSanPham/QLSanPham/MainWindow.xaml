<Window x:Class="QLSanPham.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QLSanPham"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800"
        WindowStartupLocation="CenterScreen" 
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Label" x:Key="labelsp">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Blue"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Foreground" Value="Blue"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Width" Value="85"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="#CFC"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="myheader" TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Blue"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Canvas>
        <Label Content="QUẢN LÝ SẢN PHẨM" Canvas.Left="280" Canvas.Top="14" 
               FontSize="20" FontWeight="Bold" Foreground="Blue"/>

        <Label Style="{StaticResource labelsp}" Content="Mã sản phẩm" Canvas.Left="120" Canvas.Top="70"/>
        <TextBox Name="txtmasp" Canvas.Left="250" Canvas.Top="70" Width="150" Height="25" VerticalContentAlignment="Center" IsReadOnly="True" Background="#EEEEEE"/>

        <Label Style="{StaticResource labelsp}" Content="Tên sản phẩm" Canvas.Left="120" Canvas.Top="100"/>
        <TextBox Name="txttensp" Canvas.Left="250" Canvas.Top="100" Width="150" Height="25" VerticalContentAlignment="Center"/>

        <Label Style="{StaticResource labelsp}" Content="Số lượng" Canvas.Left="120" Canvas.Top="130"/>
        <TextBox Name="txtsoluong" Canvas.Left="250" Canvas.Top="130" Width="150" Height="25" VerticalContentAlignment="Center"/>

        <Label Style="{StaticResource labelsp}" Content="Đơn giá" Canvas.Left="120" Canvas.Top="160"/>
        <TextBox Name="txtdongia" Canvas.Left="250" Canvas.Top="160" Width="150" Height="25" VerticalContentAlignment="Center"/>

        <Label Style="{StaticResource labelsp}" Content="Danh mục" Canvas.Left="120" Canvas.Top="190"/>
        <ComboBox Name="cbDanhMuc" Canvas.Left="250" Canvas.Top="190" Width="150" Height="25"/>

        <DataGrid x:Name="dgDanhsachSP" Height="240" Width="680" AutoGenerateColumns="False"
                  Canvas.Left="60" Canvas.Top="240"
                  SelectionChanged="SelectSanpham" ColumnHeaderStyle="{StaticResource myheader}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SP" Binding="{Binding MaSanPham}" Width="80"/>
                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenSanPham}" Width="180"/>
                <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong}" Width="80"/>
                <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia}" Width="100"/>

                <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat=\{0:#\,##0\}}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Red"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Thêm" Canvas.Left="150" Canvas.Top="500" Click="btnThem_Click"/>
        <Button Content="Thống kê" Canvas.Left="350" Canvas.Top="500" Click="btnThongKe_Click" Width="100"/>
        <Button Content="Đóng" Canvas.Left="550" Canvas.Top="500" Click="btnDong_Click"/>

        <Label Name="lbl_eTensp" Canvas.Left="420" Canvas.Top="100" Foreground="Red"/>
        <Label Name="lbl_eSoluong" Canvas.Left="420" Canvas.Top="130" Foreground="Red"/>
        <Label Name="lbl_eGiatien" Canvas.Left="420" Canvas.Top="160" Foreground="Red"/>
    </Canvas>
</Window>